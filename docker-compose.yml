version: '3'
services:
  web:
    build: .
    ports:
     - "80:80"
  db:
    image: "mysql:5"
    environment:
      MYSQL_ROOT_PASSWORD: 'root'
      MYSQL_DATABASE: 'drupal_db'
    restart: always
    ports:
         - "3306:3306"
    volumes:
      - ./db:/var/lib/mysql
